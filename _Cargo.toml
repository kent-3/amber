[workspace]
members = ["merkle-distributor", "snip20-reference-impl", "amburnft"]
exclude = ["contract.wasm.gz", "hash.txt"]

[profile.release]
opt-level = 3
debug = false
rpath = false
lto = true
debug-assertions = false
codegen-units = 1
panic = 'abort'
incremental = false
overflow-checks = true
